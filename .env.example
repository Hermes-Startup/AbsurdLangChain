# ========================================
# ASSESSMENT CREDENTIALS (AUTO-PROVISIONED)
# ========================================
# These credentials are automatically injected when you run:
#   yarn mission:start
#
# DO NOT manually edit these values unless instructed.
# ========================================

# Gemini API Proxy (OpenAI-compatible)
# The platform proxies Gemini 2.5 Pro through an OpenAI-compatible API
# Your API key is your candidate ID (used for usage tracking)
OPENAI_API_KEY=your-candidate-id
OPENAI_BASE_URL=https://your-proxy-url.com/v1

# Supabase Database (Temporary, Scoped Credentials)
# These credentials are scoped to your candidate schema
# They expire after your assessment window
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PRIVATE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# ========================================
# PROVISIONING CONFIGURATION
# ========================================
# This URL is injected by the assessment platform
# It points to the Quartermaster API that provisions your credentials
QUARTERMASTER_API_URL=https://your-platform.com/api/topcandidates/provision?token=your-token

# ========================================
# OPTIONAL: ADDITIONAL FEATURES
# ========================================
# These are NOT auto-provisioned. Only set if you need these features.

# SerpAPI (for Agent search features - optional)
# SERPAPI_API_KEY=your-serpapi-key

# Direct Google API (if not using proxy - NOT RECOMMENDED for assessment)
# GOOGLE_API_KEY=your-google-api-key
